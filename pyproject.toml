[tool.poetry]
name = "test-project"
version = "0.1.0"
description = "Test project for reproducing code_block bug"

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.28.0"
flask = "^2.3.0"
pydantic = "^2.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
